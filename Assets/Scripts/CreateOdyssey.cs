using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateOdyssey : MonoBehaviour
{
    public GameObject odyssey;

    // Start is called before the first frame update
    void Start()
    {
        //for (int i = 0; i < 2; ++i)
        //{
        //    GameObject obj;
        //    if (i % 2 == 0) obj = (GameObject)Instantiate(odyssey, new Vector3(-9.0f, -0.5f, 60.0f), odyssey.transform.rotation);
        //    else
        //    {
        //        obj = (GameObject)Instantiate(odyssey, new Vector3(9.0f, -0.5f, 60.0f), odyssey.transform.rotation);
        //        obj.transform.Rotate(0.0f, 180.0f, 0.0f);
        //    }
        //    obj.transform.parent = transform;
        //}
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
